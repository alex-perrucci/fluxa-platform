name: POS format once

on:
  push:
    branches:
      - agent/pos-unified-idiot-proof-workflow

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/pos-unified-idiot-proof-workflow
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.32.5
          channel: stable
          cache: true

      - name: Format changed POS files
        working-directory: apps/pos
        run: |
          dart format \
            lib/core/workflow/pos_workflow_coordinator.dart \
            lib/features/catalog/presentation/operator_cashier_screen.dart \
            lib/features/fiscal/presentation/operator_fiscal_screen.dart \
            lib/features/hospitality/presentation/operator_tables_screen.dart \
            lib/features/orders/presentation/operator_orders_screen.dart \
            lib/features/payments/presentation/quick_payment_sheet.dart \
            lib/features/printing/presentation/operator_printing_screen.dart \
            lib/features/settings/presentation/operator_diagnostics_screen.dart

      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            echo "Already formatted"
            exit 0
          fi
          git config user.name "alex-perrucci"
          git config user.email "128584526+alex-perrucci@users.noreply.github.com"
          git add apps/pos/lib/core/workflow/pos_workflow_coordinator.dart \
            apps/pos/lib/features/catalog/presentation/operator_cashier_screen.dart \
            apps/pos/lib/features/fiscal/presentation/operator_fiscal_screen.dart \
            apps/pos/lib/features/hospitality/presentation/operator_tables_screen.dart \
            apps/pos/lib/features/orders/presentation/operator_orders_screen.dart \
            apps/pos/lib/features/payments/presentation/quick_payment_sheet.dart \
            apps/pos/lib/features/printing/presentation/operator_printing_screen.dart \
            apps/pos/lib/features/settings/presentation/operator_diagnostics_screen.dart
          git commit -m "style(pos): format unified operator workflow"
          git push origin HEAD:agent/pos-unified-idiot-proof-workflow
